// REMOVE-UPDATE
/*
const taskList = document.querySelector("#task-list");

// *** Removing Elements
//taskList.remove()
//taskList.childNodes[1].remove();
//taskList.children[3].remove();
//taskList.removeChild(taskList.children[0]);//ulaştığımız nesnenin alt elemanlarını siler

// *** removing attribute
//taskList.children[0].removeAttribute("class");

// for(let i=0; i<taskList.children.length;i++)
// {
//     taskList.children[i].removeAttribute("class");
// }

// *** Replacing Elements
//const cardHeader=document.querySelector(".card-header");

// Create Element
// const h2=document.createElement("h2");
// h2.setAttribute("class","card-header");
// h2.appendChild(document.createTextNode("my list"));

// const parent=document.querySelector(".card");
// parent.replaceChild(h2,cardHeader);

// console.log(cardHeader);

// *** Classes

let val;

let link = taskList.children[0].children[0];

// val=link.className;
// val=link.classList;

link.classList.add("new");
link.classList.remove("new");

// Attributes
val= link.getAttribute("data-id");
val= link.getAttribute("href");
val= link.setAttribute("href","http://nage.com");

val=link.hasAttribute("href"); // atanmış attribte olup olmadığını control eder

console.log(val);
*/