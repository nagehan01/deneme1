// Traversing the Dom
/*
let val;

let list = document.querySelector(".list-group");
val = list;

val = list.childNodes;
val = list.childNodes[0];

val = list.childNodes[0].nodeName;
val = list.childNodes[0].nodeType;

// direk elementerle ulaşmak için
val = list.children;// html collection
val = list.children[0];
val = list.children[2];

val = list.children[2].textContent = "new item";
val = list.children[3].children;

val = list.firstChild;// ilk node text
val = list.firstElementChild;// ilk element

val = list.lastChild;// son node text
val = list.firstElementChild;// son element

val = list.parentNode;// bir üst node card nesnesi
val = list.parentElement;
val = list.parentElement.parentElement;

val = list.children[0].nextSibling;// bir sonraki elemanı karşımıza getirir
val = list.children[0].nextElementSibling;// bir sonraki elementi karşımıza getirir

val = list.children[1].previousSibling;//aynı seviyede yukarı yada aşağıya gitmek için
val = list.children[1].previousElementSibling;//aynı seviyede yukarı yada aşağıya gitmek için
//val= list.children
//console.log(val);

for (let i = 0; i < list.children.length; i++) {
    //console.log(list.children[i]);
    if(list.childNodes[i].nodeType===3){
        console.log(list.childNodes[i]);
    }
}
// Eleman sayıları için
//val=list.childElementCount;

*/
